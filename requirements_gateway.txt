# Portal Unificado CFOTech — dependencias del gateway
# pip install -r requirements_gateway.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.0
requests>=2.31.0
flask-cors>=4.0.0  # solo si se mantiene launcher.py como fallback
